#pragma once

#include <opengloves.hpp>

namespace opengloves {
  class AlphaEncoding {
  public:
    auto encodeInput(const InputData& input, uint8_t* buffer, size_t buffer_size) -> size_t {
      return 0;
    }
  };
} // namespace opengloves